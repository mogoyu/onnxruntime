name: Build onnxruntime for ARMv7l (Fixed Build Errors)

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/build-onnxruntime-armv7-cross.yml'

env:
  ONNXRUNTIME_VERSION: "1.15.0"
  PYTHON_VERSION: "3.11"
  NUMPY_VERSION: "1.23.5"

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "${{ env.PYTHON_VERSION }}"

      - name: Upgrade pip and setuptools
        run: |
          pip install --upgrade pip setuptools wheel

      - name: Add ARMHF Architecture and Sources
        run: |
          sudo dpkg --add-architecture armhf
          sudo tee /etc/apt/sources.list.d/armhf-ports.list > /dev/null << 'EOF'
          deb [arch=armhf] http://ports.ubuntu.com/ubuntu-ports jammy main restricted universe multiverse
          deb [arch=armhf] http://ports.ubuntu.com/ubuntu-ports jammy-updates main restricted universe multiverse
          deb [arch=armhf] http://ports.ubuntu.com/ubuntu-ports jammy-security main restricted universe multiverse
          EOF
          sudo apt-get update || echo "Warning: Some apt sources failed, continuing..."

      - name: Install Cross Toolchain and Dependencies
        run: |
          sudo apt-get install -y \
            gcc-arm-linux-gnueabihf \
            g++-arm-linux-gnueabihf \
            libc6-dev-armhf-cross \
            linux-libc-dev-armhf-cross \
            build-essential \
            cmake \
            git \
            wget \
            libssl-dev \
            libffi-dev \
            libeigen3-dev \
            pkg-config \
            ccache \
            gfortran \
            libglib2.0-dev \
            libglib2.0-dev:armhf || echo "Some packages skipped"

      - name: Install ARMHF Development Libraries
        run: |
          sudo apt-get install -y \
            zlib1g-dev:armhf \
            libbz2-dev:armhf \
            liblzma-dev:armhf \
            libncursesw5-dev:armhf \
            libreadline-dev:armhf \
            libsqlite3-dev:armhf \
            libgdbm-dev:armhf \
            libdb5.3-dev:armhf \
            libexpat1-dev:armhf \
            uuid-dev:armhf \
            libssl-dev:armhf \
            libffi-dev:armhf \
            libgomp1:armhf || echo "Some optional packages skipped"

      - name: Fetch ARM Python Headers
        run: |
          SYSROOT_DIR="${{ github.workspace }}/arm-sysroot"
          mkdir -p "$SYSROOT_DIR/usr/include"
          mkdir -p "$SYSROOT_DIR/usr/lib"
          cd /tmp
          sudo apt-get download python3-dev:armhf python3-minimal:armhf 2>/dev/null || true
          for deb in *.deb; do
            if [ -f "$deb" ]; then
              dpkg -x "$deb" "$SYSROOT_DIR" 2>/dev/null || true
            fi
          done
          PYTHON_H_PATH=$(find "$SYSROOT_DIR" -name "Python.h" 2>/dev/null | head -n 1)
          if [ -n "$PYTHON_H_PATH" ]; then
            PYTHON_INC_DIR=$(dirname "$PYTHON_H_PATH")
            TARGET_INC_DIR="$SYSROOT_DIR/usr/include/python3"
            mkdir -p "$(dirname "$TARGET_INC_DIR")"
            ln -sf "$PYTHON_INC_DIR" "$TARGET_INC_DIR"
            echo "PYTHON_INCLUDE_DIR=$TARGET_INC_DIR" >> "$GITHUB_ENV"
          else
            echo "PYTHON_INCLUDE_DIR=$SYSROOT_DIR/usr/include/python3" >> "$GITHUB_ENV"
          fi
          echo "SYSROOT_DIR=$SYSROOT_DIR" >> "$GITHUB_ENV"

      - name: Fetch NumPy Headers
        run: |
          pip install "numpy==${{ env.NUMPY_VERSION }}"
          NUMPY_INC=$(python3 -c "import numpy; print(numpy.get_include())")
          DEST_DIR="${{ github.workspace }}/arm-sysroot/usr/include/numpy"
          mkdir -p "$DEST_DIR"
          cp -r "$NUMPY_INC"/* "$DEST_DIR/" 2>/dev/null || true
          echo "NUMPY_INCLUDE_DIR=$DEST_DIR" >> "$GITHUB_ENV"
          echo "Copied numpy headers from $NUMPY_INC to $DEST_DIR"

      - name: Install Host Build Tools
        run: |
          pip install cython packaging sympy flatbuffers "cmake>=3.26"

      - name: Download ONNX Runtime
        run: |
          wget -q "https://github.com/microsoft/onnxruntime/archive/refs/tags/v${{ env.ONNXRUNTIME_VERSION }}.tar.gz" -O onnxruntime-src.tar.gz
          rm -rf onnxruntime
          mkdir -p onnxruntime
          tar xzf onnxruntime-src.tar.gz -C onnxruntime --strip-components=1

      - name: Create Toolchain File
        run: |
          cat > "${{ github.workspace }}/toolchain-armv7.cmake" << 'EOF'
          set(CMAKE_SYSTEM_NAME Linux)
          set(CMAKE_SYSTEM_PROCESSOR armv7l)
          set(CMAKE_C_COMPILER arm-linux-gnueabihf-gcc)
          set(CMAKE_CXX_COMPILER arm-linux-gnueabihf-g++)
          set(CMAKE_FIND_ROOT_PATH_MODE_PROGRAM NEVER)
          set(CMAKE_FIND_ROOT_PATH_MODE_LIBRARY ONLY)
          set(CMAKE_FIND_ROOT_PATH_MODE_INCLUDE ONLY)
          set(CMAKE_FIND_ROOT_PATH_MODE_PACKAGE ONLY)
          list(APPEND CMAKE_FIND_ROOT_PATH /usr/arm-linux-gnueabihf)
          list(APPEND CMAKE_FIND_ROOT_PATH $ENV{SYSROOT_DIR})
          set(onnxruntime_BUILD_TESTS OFF)
          set(onnxruntime_BUILD_UNIT_TESTS OFF)
          EOF

      - name: Find Python Library
        run: |
          PYTHON_LIB=$(find /usr -name "libpython3.11.so*" -o -name "libpython3.so*" 2>/dev/null | head -n 1)
          if [ -z "$PYTHON_LIB" ]; then
            PYTHON_LIB="/usr/lib/python3.11/config-3.11-x86_64-linux-gnu/libpython3.11.so"
          fi
          echo "PYTHON_LIBRARY=$PYTHON_LIB" >> "$GITHUB_ENV"

      - name: Build ONNX Runtime (No Tests)
        run: |
          cd onnxruntime
          rm -rf build/Linux
          echo "=== Build Configuration ==="
          echo "PYTHON_INCLUDE_DIR=${PYTHON_INCLUDE_DIR}"
          echo "NUMPY_INCLUDE_DIR=${NUMPY_INCLUDE_DIR}"
          echo "SYSROOT_DIR=${SYSROOT_DIR}"
          echo "PYTHON_LIBRARY=${PYTHON_LIBRARY}"
          echo "==========================="
          
          bash build.sh \
            --config Release \
            --update \
            --build \
            --build_wheel \
            --skip_tests \
            --skip_onnx_tests \
            --enable_pybind \
            --parallel 2 \
            --allow_running_as_root \
            --skip_submodule_sync \
            --cmake_generator "Unix Makefiles" \
            --cmake_extra_defines \
              CMAKE_TOOLCHAIN_FILE=${{ github.workspace }}/toolchain-armv7.cmake \
              CMAKE_POLICY_VERSION_MINIMUM=3.5 \
              CMAKE_SYSTEM_PROCESSOR=armv7l \
              onnxruntime_USE_FULL_PROTOBUF=OFF \
              onnxruntime_BUILD_SHARED_LIB=OFF \
              onnxruntime_DISABLE_RTTI=ON \
              onnxruntime_BUILD_TESTS=OFF \
              onnxruntime_BUILD_UNIT_TESTS=OFF \
              onnxruntime_ENABLE_CPUINFO=OFF \
              Python3_EXECUTABLE=$(which python3) \
              Python3_INCLUDE_DIR=${PYTHON_INCLUDE_DIR} \
              Python3_LIBRARY=${PYTHON_LIBRARY} \
              CMAKE_VERBOSE_MAKEFILE=ON

      - name: Check Build Output
        run: |
          echo "=== Checking build directory ==="
          ls -la onnxruntime/build/Linux/Release/ || true
          ls -la onnxruntime/build/Linux/Release/dist/ || true
          find onnxruntime/build -name "*.whl" 2>/dev/null || true
          find onnxruntime/build -name "*.log" 2>/dev/null | head -5 | xargs -r tail -100 || true

      - name: Check and Upload
        run: |
          if ls onnxruntime/build/Linux/Release/dist/*.whl 1>/dev/null 2>&1; then
            cp onnxruntime/build/Linux/Release/dist/*.whl ./
            echo "BUILD SUCCESSFUL!"
            ls -lh *.whl
          else
            echo "Build failed. Checking CMake files..."
            cat onnxruntime/build/Linux/Release/CMakeFiles/CMakeOutput.log 2>/dev/null | tail -100 || true
            cat onnxruntime/build/Linux/Release/CMakeFiles/CMakeError.log 2>/dev/null | tail -100 || true
            exit 1
          fi

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: "onnxruntime-${{ env.ONNXRUNTIME_VERSION }}-armv7l-final"
          path: "*.whl"
          retention-days: 14
